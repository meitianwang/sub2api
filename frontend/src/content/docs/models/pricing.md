# 模型与定价

## 查看模型与定价

### 方式一：模型页面（无需登录）

访问导航栏中的[「模型」]({{BASE_URL}}/models)页面，可以查看平台所有公开可用的模型信息：

- 模型按分组归类展示
- 显示每百万 Token 的输入价格和输出价格
- 显示每个模型所属的分组
- 可直观对比不同分组中相同模型的定价差异

### 方式二：通过 API 查询

使用 API Key 查询当前 Key 所属分组的可用模型列表：

```bash
curl {{BASE_URL}}/v1/models \
  -H "Authorization: Bearer sk-xxxx"
```

将 `sk-xxxx` 替换为你的实际 API Key。返回结果为该 Key 所属分组中所有可用模型的列表。

## 计费说明

按 Token 用量计费，费用从账户余额中实时扣除。

- 输入 Token 和输出 Token 分别计价，两者价格可能不同
- 不同分组对同一模型的定价可能不同，具体以[「模型」]({{BASE_URL}}/models)页面展示的价格为准

**查看使用记录：**

登录控制台，进入「活动日志」页面，可以查看每一次 API 调用的详细信息：

- 调用的模型名称
- 输入/输出 Token 用量
- 本次请求费用
- 请求状态（成功/失败）

## 余额管理

- 充值后余额立即到账，可在控制台首页查看当前余额
- 每次 API 调用完成后，费用实时从余额中扣除
- 余额不足时，API 请求将返回错误，请及时[充值]({{BASE_URL}}/purchase)
- 可在创建 API Key 时设置额度上限，防止单个 Key 消耗过多余额
